services:
  db:
    image: postgres:16.1
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - pgdata:/var/lib/postgresql/data/
    ports:
      - "127.0.0.1:5432:5432"
    healthcheck:
      test: pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB
      interval: 5s
      timeout: 10s
      retries: 5
      start_period: 10s

  redis:
    image: redis:7.2-alpine
    restart: unless-stopped
    volumes:
      - redisdata:/data
    ports:
      - "127.0.0.1:6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  # Monolithic Papermerge container: nginx + core API + auth-server under
  # supervisord. On first boot the entrypoint runs migrations, syncs
  # permissions, creates the admin superuser, seeds default roles
  # (admin / worker / modder) and assigns the admin role to the superuser —
  # everything is idempotent.
  app:
    build:
      context: .
      dockerfile: docker/app/Dockerfile
    restart: unless-stopped
    ports:
      - "${APP_PORT:-12000}:80"
    environment:
      PAPERMERGE__SECURITY__SECRET_KEY: ${SECRET_KEY}
      PAPERMERGE__MAIN__API_PREFIX: /api
      PAPERMERGE__DATABASE__URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
      PAPERMERGE__MAIN__MEDIA_ROOT: /var/media/pmg
      PAPERMERGE__REDIS__URL: redis://redis:6379/0
      PAPERMERGE__OCR__LANG_CODES: ${OCR_LANGS:-deu,eng,rus}
      PAPERMERGE__OCR__DEFAULT_LANG_CODE: ${OCR_DEFAULT_LANG:-rus}
      PAPERMERGE__OCR__MULTI_LANG_CODES: ${OCR_MULTI_LANGS:-eng+rus}
      PAPERMERGE__OCR__AUTOMATIC: ${OCR_AUTOMATIC:-false}
      PAPERMERGE__AUTH__USERNAME: ${ADMIN_USERNAME}
      PAPERMERGE__AUTH__EMAIL: ${ADMIN_EMAIL}
      PAPERMERGE__AUTH__PASSWORD: ${ADMIN_PASSWORD}
    volumes:
      - ./media:/var/media/pmg
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  ocr_worker:
    build:
      context: .
      dockerfile: docker/ocrworker/Dockerfile
    command: worker
    restart: unless-stopped
    environment:
      PAPERMERGE__DATABASE__URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
      PAPERMERGE__REDIS__URL: redis://redis:6379/0
      PAPERMERGE__MAIN__MEDIA_ROOT: /var/media/pmg
      PAPERMERGE__OCR__MULTI_LANG_CODES: ${OCR_MULTI_LANGS:-eng+rus}
      OCR_WORKER_ARGS: -Q ocr -c ${OCR_WORKER_CONCURRENCY:-2}
    volumes:
      - ./media:/var/media/pmg
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

volumes:
  pgdata:
  redisdata:
