/** docx-preview `className` option; each page is `section.{DOCX_VIEWER_CLASS}`. */
export const DOCX_VIEWER_CLASS = "docx-docviewer"

/** Added to each logical page `<section>` after render (reliable scroll / thumbnail targeting). */
export const DOCX_VIEWER_PAGE_CLASS = "docx-viewer-page"
